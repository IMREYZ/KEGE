# Шаблон для 17 задания

array = [int(x) for x in open('17.txt')]
answer = []

for i in range(len(array) - 1): # Перебираем пары
    if ...:
        answer += [....]

print(min(answer), max(answer), len(answer))
